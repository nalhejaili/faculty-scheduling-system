<UserControl x:Class="MiniTrainerScheduler.Views.MasterBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             FlowDirection="LeftToRight"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">

    <UserControl.Resources>
        
        <Style x:Key="CellBorder" TargetType="Border">
            <Setter Property="BorderBrush" Value="#D8DFE8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
        </Style>

        
        <Style x:Key="CellText" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        
        <Style x:Key="HeaderText" TargetType="TextBlock" BasedOn="{StaticResource CellText}">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#F3F6FA"/>
        </Style>

        
        <SolidColorBrush x:Key="DeptCardBgBrush"     Color="#F1F5FF"/>
        <SolidColorBrush x:Key="DeptCardBorderBrush" Color="#C7D2FE"/>

        
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="{DynamicResource DeptCardBgBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DeptCardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Sun -->
        <Style x:Key="CardBorder.HideIfEmpty_Sun1" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Sun.Track1}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Sun.Track1}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Sun2" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Sun.Track2}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Sun.Track2}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Sun3" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Sun.Track3}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Sun.Track3}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Mon -->
        <Style x:Key="CardBorder.HideIfEmpty_Mon1" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Mon.Track1}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Mon.Track1}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Mon2" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Mon.Track2}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Mon.Track2}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Mon3" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Mon.Track3}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Mon.Track3}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Tue -->
        <Style x:Key="CardBorder.HideIfEmpty_Tue1" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tue.Track1}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tue.Track1}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Tue2" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tue.Track2}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tue.Track2}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Tue3" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tue.Track3}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tue.Track3}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Wed -->
        <Style x:Key="CardBorder.HideIfEmpty_Wed1" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Wed.Track1}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Wed.Track1}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Wed2" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Wed.Track2}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Wed.Track2}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Wed3" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Wed.Track3}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Wed.Track3}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Thu -->
        <Style x:Key="CardBorder.HideIfEmpty_Thu1" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Thu.Track1}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Thu.Track1}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Thu2" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Thu.Track2}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Thu.Track2}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardBorder.HideIfEmpty_Thu3" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Thu.Track3}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Thu.Track3}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    
    <Border x:Name="MasterTemplateRoot" BorderBrush="#999" BorderThickness="2" Padding="8">
        <Grid Margin="0" Grid.IsSharedSizeScope="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" SharedSizeGroup="TimeCol"/>
                
                <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                
                <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                
                <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                
                <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                
                <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                
            </Grid.ColumnDefinitions>

            
            <Border Style="{StaticResource CellBorder}" Grid.Column="0">
                <TextBlock Style="{StaticResource HeaderText}" Text="Time"/>
            </Border>
            <Border Style="{StaticResource CellBorder}" Grid.Column="1">
                <TextBlock Style="{StaticResource HeaderText}" Text="Sunday"/>
            </Border>
            <Border Style="{StaticResource CellBorder}" Grid.Column="2">
                <TextBlock Style="{StaticResource HeaderText}" Text="Monday"/>
            </Border>
            <Border Style="{StaticResource CellBorder}" Grid.Column="3">
                <TextBlock Style="{StaticResource HeaderText}" Text="Tuesday"/>
            </Border>
            <Border Style="{StaticResource CellBorder}" Grid.Column="4">
                <TextBlock Style="{StaticResource HeaderText}" Text="Wednesday"/>
            </Border>
            <Border Style="{StaticResource CellBorder}" Grid.Column="5">
                <TextBlock Style="{StaticResource HeaderText}" Text="Thursday"/>
            </Border>

            
            <ScrollViewer Grid.Row="1" Grid.ColumnSpan="6"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding TemplateRows}"
                              HorizontalContentAlignment="Stretch">
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            
                            <Grid Margin="0,6,0,0" HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140" SharedSizeGroup="TimeCol"/>
                                    
                                    <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                                    
                                    <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                                    
                                    <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                                    
                                    <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                                    
                                    <ColumnDefinition Width="*" SharedSizeGroup="DayCol"/>
                                    
                                </Grid.ColumnDefinitions>

                                
                                <Border Style="{StaticResource CellBorder}" Grid.Column="0" MinHeight="84">
                                    <TextBlock Style="{StaticResource CellText}" Text="{Binding Time}"/>
                                </Border>

                                
                                <Border Style="{StaticResource CellBorder}" Grid.Column="1" MinHeight="84">
                                    <StackPanel>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Sun1}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Sun.Track1}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Sun2}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Sun.Track2}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Sun3}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Sun.Track3}"/>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                
                                <Border Style="{StaticResource CellBorder}" Grid.Column="2" MinHeight="84">
                                    <StackPanel>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Mon1}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Mon.Track1}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Mon2}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Mon.Track2}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Mon3}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Mon.Track3}"/>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                
                                <Border Style="{StaticResource CellBorder}" Grid.Column="3" MinHeight="84">
                                    <StackPanel>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Tue1}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Tue.Track1}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Tue2}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Tue.Track2}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Tue3}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Tue.Track3}"/>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                
                                <Border Style="{StaticResource CellBorder}" Grid.Column="4" MinHeight="84">
                                    <StackPanel>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Wed1}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Wed.Track1}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Wed2}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Wed.Track2}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Wed3}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Wed.Track3}"/>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                
                                <Border Style="{StaticResource CellBorder}" Grid.Column="5" MinHeight="84">
                                    <StackPanel>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Thu1}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Thu.Track1}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Thu2}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Thu.Track2}"/>
                                        </Border>
                                        <Border Style="{StaticResource CardBorder.HideIfEmpty_Thu3}">
                                            <TextBlock Style="{StaticResource CellText}" Text="{Binding Thu.Track3}"/>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
