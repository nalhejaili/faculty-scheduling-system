<Window x:Class="MiniTrainerScheduler.Views.SetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Password"
        Height="260" Width="420"
        FlowDirection="LeftToRight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F9FB">

    <Border Margin="16" Background="White" Padding="16" CornerRadius="10"
            BorderBrush="#E5E7EB" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"/>

            <StackPanel Grid.Row="1" Margin="0,12,0,0">
                <TextBlock Text="New Password" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,6,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,10,0,0">
                <TextBlock Text="Confirm Password" FontWeight="SemiBold"/>
                <PasswordBox x:Name="ConfirmBox" Margin="0,6,0,0"/>
            </StackPanel>

            <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="#B91C1C" Margin="0,10,0,0" TextWrapping="Wrap"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
                <Button Content="Save" Padding="14,6" Click="Save_Click"/>
                <Button Content="Cancel" Padding="14,6" Margin="6,0,0,0" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
